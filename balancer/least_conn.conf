http {
    upstream app_servers {
        least_conn;
        server webapp:8080;
    }

    server {
        listen 8080;

        location / {
            proxy_pass http://app_servers;
        }
    }
}
